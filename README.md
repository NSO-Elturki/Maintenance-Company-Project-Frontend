# CarMaintenanceFrontend

- This project use Angular version 19.1.5 and Node version 22.11.0

- Before start run npm install


To start the fake backend development server, run:

```bash
npm run server
```

To start the fake backend development server, run:

```bash
npm run server
```

To start a local development server, run:

```bash
ng serve
```
